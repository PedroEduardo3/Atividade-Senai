using static System.Console;
int começo, soma;

WriteLine("Coloque um valor");
começo = Convert.ToInt32(ReadLine());

for (int i = 0; i <= 10; i++)
{
    soma = i * começo;
    WriteLine($"{i} x {começo} = {soma}");
}
